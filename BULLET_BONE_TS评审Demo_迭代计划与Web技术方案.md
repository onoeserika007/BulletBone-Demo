# BULLET BONE TS 评审 Demo：收敛版迭代计划与 Web 技术方案

**需求基线：** `demo制作.docx`  
**Demo 性质：** 评审现场使用的可玩 Mini-Run，不是技术验证。  
**首要指标：** 评委看起来玩得爽，同时能够完整看到“开局选择—战斗—成长—商人/宝箱—Boss—结算”。  
**制作约束：** 用户不提供任何素材；美术、UI、特效和音频全部由制作过程自行解决。  
**计划周期：** 3 个核心开发日；第 4 日仅作为可选稳定与表现缓冲。

---

## 1. `demo制作.docx` 提炼出的唯一需求范围

### 1.1 武器

只实现两把：

1. **连发激光步枪**：初始武器，高射速直线激光；
2. **重力场枪**：低射速，发射重力炸弹；命中敌人或掩体后坍缩，并牵引周围敌人。

为了节省成本：

- 不做完整武器品质和随机掉落；
- 重力场枪固定为橙色武器；
- 重力场枪固定携带词条和特殊效果；
- 不实现更多武器。

### 1.2 核心机制

- 共 5 个印记槽；
- 常态击杀敌人获得 1 个印记；
- 常态成功格挡怪物攻击获得 1 个印记；
- 满 5 个印记后自动进入狂暴状态；
- 狂暴只提高射速和伤害；
- 狂暴期间击杀敌人掉落印记碎片；
- 拾取印记碎片增加当前狂暴剩余时间；
- 不实现除此之外的印记和觉醒扩展链路。

### 1.3 Build

- 芯片只做 1～2 个流派，保证容易形成 Build；
- 玩家特质分为 5 个分支；
- 开局从 5 个分支中随机给出 3 个，玩家选择 1 个；
- 每个战斗房清场后选择一次词条，再进入下一关；
- 所有成长优先采用简单数值变化和少量可见效果。

### 1.4 关卡与节点

- 整体线性推进；
- 节点只展示商人和宝箱；
- 不做复杂分叉和随机拓扑；
- 必须实际跑通房间推进、清场、词条选择、商人、宝箱、Boss 和结算。

### 1.5 怪物

只做 3 种：

1. **近战怪**：追逐玩家并近身攻击；
2. **远程怪**：尝试与玩家保持距离，使用带红色预警线的瞬时激光；
3. **Boss**：必须有 1 个技能，有余量再增加第 2 个。

---

## 2. 为避免歧义采用的实现假设

需求未明确的地方采用最省工、最稳定的实现：

- 初始武器固定为连发激光步枪；
- 宝箱固定获得橙色重力场枪，获得后自动装备；
- 玩家保留两把武器，按 `1` / `2` 切换；两把武器均使用无限弹药与射击冷却，不实现弹夹和备用弹药；
- 商人不做货币经济，直接提供一次芯片 3 选 1；
- 成功格挡会抵消攻击并立即向瞄准方向反射一道骨光，不做骨弹储存槽；
- 5 印记满后自动进入 6 秒狂暴，不再额外按键激活；
- 狂暴只增加射速和伤害；
- 狂暴期间每次击杀必掉 1 枚印记碎片，拾取后增加 1 秒狂暴时间；
- 印记碎片具有小范围磁吸，避免精确踩中；
- 狂暴结束后印记归零，场上未拾取碎片消失；
- 房间结束后玩家生命、特质、芯片、词条和当前武器保留；
- 玩家死亡只显示失败页并允许立即重开，不做局外成长。

这些假设都集中配置，后续可以低成本调整数值；开工后不再扩展玩法范围。

### 2.1 开工前冻结的运行规则

为避免场景切换、计时和格挡判定出现歧义，冻结以下规则：

- 狂暴期间击杀不再增加印记，只掉落 1 枚印记碎片；
- 同一敌人只在首次进入死亡状态时结算一次击杀或碎片；
- 同一次近战攻击或激光施放最多奖励 1 个格挡印记，三线激光不能一次获得 3 个；
- 狂暴剩余时间只在战斗房 Gameplay 运行时递减；词条选择、商人、宝箱、场景淡入淡出和暂停期间不递减；
- 狂暴、印记、生命、武器和 Build 都由 `RunState` 持有；`RoomScene` 只持有当前房的敌人、攻击、弹丸和碎片对象；
- 满 5 印记时如果战斗房已经清场，狂暴进入待触发状态，并在下一个战斗房开始时自动激活，避免浪费；
- 最后一名敌人死亡后延迟 1 秒再弹出词条；期间保留玩家控制并提高碎片磁吸，确保最后一枚续时碎片有拾取机会；
- 房间切换时清除当前房的敌人、弹丸、碎片、粒子、Timer、Tween、输入监听和事件监听；
- 卡片界面出现时暂停 Gameplay 输入和计时，选择结果写回 `RunState`，UI 不保存第二份状态；
- 开局特质必须从 5 项中无重复抽取 3 项；随机只使用当前 Run 的单一 RNG，不直接调用 `Math.random()`。

### 2.2 格挡与瞬时激光判定

远程怪和 Boss 的瞬时激光不使用“生成后再碰撞”的弹丸逻辑，而采用统一攻击结算：

```text
显示红色预警线
→ 预警结束时生成 IncomingAttack
→ 检查玩家是否处于格挡窗口、距离与朝向是否合法
→ 成功：取消伤害、播放格挡反馈、发射反射骨光；仅常态印记 +1
→ 失败：应用伤害
```

近战攻击也调用同一结算入口。这样可以保证预警线、伤害、格挡和印记不会各自维护一套判断。

---

## 3. 最终 Mini-Run 流程

### 3.1 操作映射

| 操作 | 按键 |
|---|---|
| 移动 | `WASD` |
| 瞄准 | 鼠标 |
| 射击 | 鼠标左键 |
| 格挡并即时反击 | 鼠标右键 |
| 切换激光步枪 | `1` |
| 切换重力场枪 | `2` |
| 暂停 | `Esc` |

不实现换弹、翻滚、交互键和手动狂暴键。

### 3.2 关卡顺序

```text
标题页
→ 开局特质随机 3 选 1
→ 战斗房 1：近战怪教学
→ 词条 3 选 1
→ 商人节点：芯片 3 选 1
→ 战斗房 2：近战怪 + 远程激光怪
→ 词条 3 选 1
→ 宝箱节点：固定获得橙色重力场枪
→ 战斗房 3：使用重力枪聚怪，触发格挡与狂暴
→ 词条 3 选 1
→ Boss 房
→ 胜利结算
```

目标总时长：**3～5 分钟**。

建议时间预算：

| 环节 | 目标时长 |
|---|---:|
| 标题与特质选择 | 15～20 秒 |
| 战斗房 1 | 30～35 秒 |
| 词条 + 商人 | 20～25 秒 |
| 战斗房 2 | 35～40 秒 |
| 词条 + 宝箱 | 15～20 秒 |
| 战斗房 3 | 40～50 秒 |
| 词条 + Boss 过场 | 15～20 秒 |
| Boss | 50～70 秒 |
| 结算 | 10 秒 |

### 3.3 流程表现方式

- 不制作复杂节点地图；
- 使用一条横向线性进度条展示当前节点和后续节点；
- 商人和宝箱使用独立全屏卡片界面；
- 战斗房复用同一 `RoomScene`，通过障碍布局、背景色和波次区分；
- 清场后直接弹出词条选择，确认后进入下一节点；
- 场景切换使用 200～300ms 黑幕淡入淡出。

---

## 4. 战斗与成长的最小实现

## 4.1 连发激光步枪

- 按住左键连续射击；
- 直线高速弹道；
- 使用短激光线段表现，不制作复杂光束 Shader；
- 命中包含闪白、火花、伤害数字、轻震屏；
- 使用无限弹药，只保留射击间隔，不实现换弹。

## 4.2 重力场枪

- 低射速；
- 发射一枚可见重力弹；
- 命中敌人、墙体或到达最大距离时坍缩；
- 坍缩范围内敌人暂停自主移动和攻击，速度转向坍缩中心；
- 坍缩结束造成一次范围伤害，存活敌人恢复 AI；
- 每枚重力弹只能坍缩和结算一次，死亡敌人不再参与牵引；
- 使用扩散圆、收缩圆、粒子向中心移动实现视觉效果；
- 不使用真实引力模拟、流体、布料、复杂物理或自定义 Shader。

固定橙色效果：

- **重力过载：** 坍缩半径增加，并在结束时追加一次爆炸；
- 固定词条：伤害或范围提升，不做随机抽取。

## 4.3 格挡

评审 Demo 保留“防御转进攻”的观感，但压缩正式策划中的骨弹储存与二次释放：

- 右键触发 0.45 秒前方格挡窗口；
- 格挡期间锁定射击，移动速度降为 0；
- 格挡判定使用“距离 + 朝向夹角”，不制作复杂碰撞扇形；
- 成功时抵消一次近战攻击或一次激光施放；
- 同一次攻击施放最多结算一次成功格挡；
- 成功反馈：短暂停顿、骨白圆环、高频“叮”、常态下印记槽 +1；
- 成功后立即向准星方向反射一道高伤骨光，表达“接住并打回去”；
- 成功格挡冷却 0.35 秒，失败格挡冷却 1 秒；
- 狂暴期间仍可格挡和反击，但不再增加印记；
- 不做独立骨弹储存、蓄能和二次释放。

该方案是评审版对正式格挡案的明确压缩，不代表正式开发规则变更。

## 4.4 印记与狂暴

印记只属于玩家，不附着到敌人：

```text
常态击杀敌人 +1 印记
或成功格挡 +1 印记
→ 印记达到 5
→ 自动进入狂暴 6 秒
→ 狂暴期间击杀敌人掉落印记碎片
→ 拾取 1 枚碎片，当前狂暴剩余时间 +1 秒
→ 狂暴结束，印记清零并删除未拾取碎片
```

狂暴的 Gameplay 效果只保留：

- 射速 +50%；
- 伤害 +30%。

狂暴的最低限度表现：

- 玩家变为骨白高亮；
- 激光宽度和命中粒子增大；
- HUD 显示狂暴剩余时间；
- 印记碎片使用发光菱形和小范围磁吸。

为了让评审看到续时形成的连续击杀节奏，狂暴战斗波次会安排低血量敌人。碎片每次只增加固定时间，不叠加其他属性，不建立独立碎片库存。

明确删除：

- 敌人身上的多层印记；
- 印记衰减；
- 击杀引爆；
- 常态击杀掉落碎片；
- 独立觉醒槽；
- 手动 E 键觉醒；
- 狂暴移速和减伤加成；
- 复杂续时倍率、掉落概率和碎片库存。

## 4.5 特质、芯片和词条

### 开局特质：5 个分支

每局从以下 5 项中随机展示 3 项：

1. **速射：** 激光步枪射速 +20%；
2. **重力：** 重力场半径 +25%；
3. **格挡：** 格挡窗口 +0.15 秒；
4. **狂暴：** 狂暴持续时间 +2 秒；
5. **生存：** 最大生命 +25%。

### 芯片：2 个流派

只做 4 张，商人从中固定展示 3 张：

**格挡流：**

- 骨壳强化：格挡窗口扩大；
- 反击增幅：反射骨光伤害提高。

**狂暴流：**

- 狂暴核心：狂暴射速额外提高；
- 延时狂暴：狂暴持续时间增加。

### 房间词条

每个战斗房后从以下简单词条中展示 3 项：

- 伤害 +20%；
- 射速 +15%；
- 移速 +10%；
- 格挡冷却 -25%；
- 重力半径 +20%；
- 最大生命 +20%。

不实现复杂触发器、品质权重、同类限制、词条池持久化和局外解锁。

所有纯数值成长采用同一计算规则：同类百分比先相加，再基于基础值计算一次最终值；持续时间和格挡窗口采用固定值相加。选择卡片只向 `RunState` 写入 Modifier，不能直接重复修改 Player 当前值，避免跨房间重复叠加。

---

## 5. Boss 最小方案

### 必做技能：预警激光扫射

```text
红色预警线锁定
→ 0.7 秒准备
→ 发射高亮激光
→ 玩家走位或格挡
```

Boss 循环使用不同角度的单线或三线激光。该技能复用远程怪的激光代码，只调整数量、角度和伤害。

### 有余量再做：重力炮

- Boss 发射慢速重力弹；
- 落地后显示收缩范围；
- 轻微牵引玩家；
- 延迟爆炸。

如果第 3 日中午前主流程仍不稳定，直接删除第二技能。

---

## 6. 明确删除的旧计划内容

以下内容因 `demo制作.docx` 未要求而全部删除：

- 复杂节点地图、路线分叉、节点生成和汇流；
- 金币、价格、商店购买和经济系统；
- 精英房、休整点、医疗站和事件房；
- 多房间随机模板；
- 流浪者、枪手之外的普通敌人；
- 完整武器品质和随机掉落；
- 6 武器、15 词条、27 芯片和 12 特质内容量；
- 敌人印记、死亡引爆、常态碎片掉落和独立觉醒槽；
- 骨弹储存与蓄能释放；
- 对象池通用框架、ECS 和通用 Ability 系统；
- 存档、局外成长、Seed、回放和统计上传；
- 自定义 Shader、复杂后处理、动态光照、程序化破碎；
- 移动端、手柄、联网和后端。

---

## 7. Web 技术栈

- TypeScript（`strict`）；
- Vite；
- Phaser 3；
- Phaser Arcade Physics；
- Phaser Scene、Camera、Tween、Particle、Sound；
- HTML/CSS：特质、词条、商人、宝箱和结算；
- 浏览器本地运行，不接后端。

不引入 React、Redux、Zustand、Matter.js、ECS、依赖注入和大型 UI 框架。

### 最小工程结构

```text
src/
├── main.ts
├── scenes/
│   ├── MenuScene.ts
│   ├── RoomScene.ts
│   └── ResultScene.ts
├── run/
│   ├── RunState.ts
│   ├── RunRng.ts
│   └── LinearRunFlow.ts
├── combat/
│   ├── Player.ts
│   ├── Enemy.ts
│   ├── Boss.ts
│   ├── IncomingAttack.ts
│   ├── LaserRifle.ts
│   ├── GravityGun.ts
│   ├── BlockController.ts
│   └── RageController.ts
├── config/
│   └── demoConfig.ts
└── ui/
    ├── Hud.ts
    └── ChoiceOverlay.ts
```

不再需要独立 `MapScene`。线性节点进度条由 HTML/CSS 覆盖层展示，`LinearRunFlow` 决定下一个房间。

### 状态和生命周期边界

- `demoConfig.ts`：只读基础数值和房间定义；
- `RunState`：当前生命、武器、特质、芯片、词条、印记、狂暴状态和流程进度的唯一真源；
- `RunRng`：本局特质抽取的唯一随机入口；
- `RoomScene`：当前房间运行时对象及清场条件；
- `IncomingAttack`：近战、远程激光和 Boss 激光的统一攻击描述与格挡结算；
- `Hud` / `ChoiceOverlay`：只读取状态并发送选择命令，不直接持有 Gameplay 真值；
- `RoomScene` 的 `shutdown` 必须统一注销 Timer、Tween、输入和事件，并清空物理 Group。

---

## 8. 素材自给方案

用户不提供任何素材，采用以下方式：

### 程序化完成

- 房间地面、墙体、障碍和门；
- 激光、预警线、重力场、格挡圆环和粒子；
- HUD、印记槽、节点进度条、卡片和结算；
- 伤害数字、闪白、后坐、震屏和淡入淡出。

### AI 生成静态素材

- 主角；
- 近战怪；
- 远程怪；
- Boss；
- 连发激光步枪；
- 重力场枪；
- 商人、宝箱和标题页静态图。

不生成完整八方向或逐帧动画。动作使用 Sprite 翻转、武器旋转、后坐、浮动、闪白、缩放和击退表达。

### 外部许可证素材

- 音效优先使用 CC0；
- 字体使用系统字体或 OFL 字体；
- 所有外部文件记录到 `ASSET_LICENSES.md`；
- 不使用 Excel、Word 参考图里的其他游戏截图作为运行素材。

---

## 9. 三日核心迭代计划

## Day 1：完整线性流程 + 基础战斗

### 目标

先用程序图形和占位 UI 跑通整局，不等待生成素材。

### 任务

- 初始化 Phaser + TypeScript + Vite；
- 建立 `MenuScene`、`RoomScene`、`ResultScene`；
- 建立 `RunState` 和 `LinearRunFlow`；
- 跑通标题、特质选择、3 个战斗房、商人、宝箱、Boss、结算；
- 实现移动、瞄准和连发激光步枪；
- 实现近战怪；
- 实现清场、词条选择和下一关；
- 建立基础 HUD 和线性节点进度条；
- 并行生成 4 个角色静态素材初稿。

### 当日验收

- 可以从标题一路走到结算；
- 战斗房能够封锁、清场、选词条并离开；
- 特质、词条和生命能够跨房间保存；
- 激光步枪可以稳定击杀近战怪。

### 取舍

不做精细美术、声音和复杂特效；不能删除完整线性流程。

---

## Day 2：核心机制 + 第二武器 + Build

### 目标

完成评审版的主要差异化内容。

### 任务

- 实现远程怪保持距离；
- 实现红色预警线和瞬时激光；
- 实现格挡、反射骨光和格挡印记；
- 实现击杀印记、5 槽和自动狂暴；
- 实现狂暴击杀掉落印记碎片、磁吸拾取和狂暴时间增加；
- 实现重力场枪、牵引和坍缩爆炸；
- 实现 5 个开局特质；
- 实现 4 张芯片和基础词条；
- 实现商人芯片选择和宝箱固定橙色武器；
- 加入命中、格挡、重力坍缩和狂暴的基础反馈；
- 替换第一批生成静态素材。

### 当日验收

- 常态击杀和格挡均可正确增加印记；
- 第 5 个印记稳定触发狂暴，狂暴只修改射速和伤害；
- 狂暴期间击杀必掉碎片，拾取后剩余时间正确增加，结束后状态与碎片正确清理；
- 重力枪可以牵引至少 3 个敌人并造成范围击杀；
- 商人和宝箱奖励能影响后续战斗；
- 房间切换后敌人、激光和计时器不残留。

### 取舍

重力牵引只修改敌人速度，不做真实物理；格挡只使用距离和夹角；芯片与词条直接修改数值。

---

## Day 3：Boss + 爽感 + 稳定交付

### 目标

形成可连续演示的 3～5 分钟评审版本。

### 任务

- Boss 血量、出场和预警激光技能；
- 有余量再做重力炮；
- Boss 死亡和胜利结算；
- 统一骨白、锈红、暗灰和激光红配色；
- 补充枪口、命中、震屏、短暂停顿和粒子；
- 补充格挡“叮”、狂暴和重力坍缩音效；
- 加入 `F2` 重置房间、`F3` 立即狂暴、`F4` 跳转 Boss；
- 至少连续彩排 10 次；
- 修复阻断流程的问题；
- 生成本地静态构建。

### 最终验收

- 10 次完整彩排中至少 9 次能走到结算；
- 开局随机 3 选 1 正常；
- 三个战斗房均能完成清场和词条选择；
- 商人与宝箱节点可以正常完成；
- 两把武器差异肉眼可见；
- 格挡、狂暴和重力坍缩至少各出现一次高光；
- Boss 至少有一个稳定可读的技能；
- Chrome 和 Edge 无阻断错误。

---

## 10. 可选第 4 日：只做表现缓冲

仅在前三日主流程已经稳定时进行：

- Boss 第二技能；
- 更好的角色静态图；
- 更清晰的房间背景；
- 补充音效；
- 调整震屏、Hit Stop 和粒子数量；
- 调整怪物血量和房间时长。

第 4 日不增加新系统。

---

## 11. 开工前最终 Review 结论

### 11.1 需求优先级

发生冲突时按以下顺序执行：

1. 本轮对话中已经确认的最新规则；
2. `demo制作.docx` 的评审 Demo 范围；
3. `BULLET_BONE_骨塔传说_策划案_v1.xlsx` 的完整产品设计；
4. `BULLET_BONE_技术方案_讨论稿_正式开发.md` 的 UE 正式开发架构。

### 11.2 相对完整策划案的有意偏差

| 完整策划案 | 评审 Demo | 结论 |
|---|---|---|
| 多种常规枪械 | 激光步枪 + 重力场枪 | 以 `demo制作.docx` 为准 |
| 敌人叠加骨蚀印记 | 玩家持有 5 个印记槽 | 以最新简化规则为准 |
| 碎片用于充满独立觉醒槽 | 狂暴击杀碎片只延长狂暴时间 | 以最新简化规则为准 |
| 手动触发觉醒且包含多项增益 | 满 5 印记自动狂暴，只加射速和伤害 | 以最新简化规则为准 |
| 格挡吸收并储存骨弹，再主动释放 | 成功格挡后即时反射骨光 | 评审版压缩，正式案不变 |
| 可见分支节点图 | 线性进度条，仅展示商人和宝箱 | 以 `demo制作.docx` 为准 |
| 完整商店经济 | 商人免费芯片 3 选 1 | 评审版压缩 |

这些偏差必须在评审时被表述为“Demo 范围裁剪”，不能作为正式策划已变更的依据。

### 11.3 从正式技术框架保留的原则

- `RunState`、房间运行时和 UI 展示状态分离；
- Gameplay 状态只有一个真源，UI 不复制生命、印记和狂暴状态；
- Run、Room 和 Actor 生命周期分离；
- 房间清场条件由房间/遭遇逻辑判断，不由单个敌人直接推进流程；
- 明确所有权时使用直接调用和局部回调，不建立全局 EventManager；
- Gameplay 随机统一经过 `RunRng`；
- 房间退出时完整清理攻击、弹丸、Timer、Tween、输入和事件；
- Gameplay 时间与 UI 选择时间分离。

### 11.4 明确不搬用的正式架构

- UE5、Paper2D、PaperZD、GAS、Lua、Blueprint 和 GameplayTag；
- Streaming Level、Subsystem、UObject、AbilityTask 和 GameplayCue；
- 正式版对象池、资源异步加载、存档和配置导入框架；
- 完整 ShotSpec、Effect、Attribute 和领域消息系统。

### 11.5 开工门禁检查

以下项目已冻结，可以开工：

- [x] 两把武器的获得和切换方式；
- [x] 三类怪物及 Boss 最低技能数；
- [x] 格挡窗口、硬直、冷却和即时反击；
- [x] 5 印记、自动狂暴、狂暴增益和碎片续时；
- [x] 线性 Run、三个战斗房、商人、宝箱、Boss 和结算；
- [x] 特质、芯片和词条的最小内容量；
- [x] Run/Room/UI 状态所有权；
- [x] 瞬时激光与格挡的统一判定；
- [x] 房间切换清理规则；
- [x] 素材自给方案和效果上限；
- [x] 3 日主计划与第 4 日降级原则。

**Review 结论：** 当前策划范围与 Web 技术框架可以开工。P0 规则冲突已经消除；剩余风险主要是三日内的素材一致性和表现打磨，不再通过增加系统解决。

---

## 12. 最终交付定义

最终交付必须满足：

- 是完整线性 Mini-Run，不是战斗 Gym；
- 包含开局特质、三个战斗房、房后词条、商人、宝箱、Boss 和结算；
- 只包含两把武器和三种怪物；
- 核心资源简化为 5 个玩家印记槽；
- 常态击杀和格挡积累印记，满 5 自动狂暴；
- 狂暴只增加射速和伤害，狂暴击杀掉落的印记碎片用于延长狂暴时间；
- 芯片只表现 1～2 个易成型流派；
- 所有效果都限定在 Phaser 基础能力和可自行生成的素材范围内；
- 不再恢复本需求没有提到的系统。
