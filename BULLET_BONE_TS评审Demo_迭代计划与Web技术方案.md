# BULLET BONE TS 评审 Demo：当前迭代计划

**状态：** 基础 Mini-Run 已完成，进入枪械系统与表现扩展阶段。  
**需求优先级：** 最新试玩反馈 > `demo制作.docx` > 完整策划案。  
**目标：** 保持完整可玩流程，强化枪械差异、怪物掉落、格挡反馈和规则可读性。

## 1. 当前已完成基线

- Phaser 3 + TypeScript + Vite；
- 六个普通战斗房、商人、宝箱、Boss 和结算；
- 近战怪、远程预警激光怪、Boss；
- 格挡、即时反击、冷却倒计时；
- 5 印记自动狂暴；
- 狂暴击杀掉落碎片，触碰碎片延长狂暴；
- 连发激光枪与重力场枪；
- GitHub Pages 自动部署。

## 2. 本轮冻结规则

### 2.1 印记与狂暴

```text
常态击杀或成功格挡 +1 印记
→ 满 5 自动狂暴
→ 狂暴只增加伤害和射速
→ 狂暴击杀掉落碎片
→ 玩家主动触碰碎片，狂暴时间 +1 秒
```

- 印记碎片不吸附；
- HUD 常驻说明印记来源、满层效果和碎片用途；
- 清场后生成出口光点，玩家靠近并按 `E` 才进入词条选择和下一关；
- 离开房间时清空所有未拾取的印记碎片和枪械掉落物。

### 2.2 格挡

- 右键展开格挡；
- 成功格挡取消伤害并立即反击；
- 成功反馈统一为蓝色扇区、蓝色冲击环、蓝白反击光和音效；
- HUD 显示格挡就绪、展开和冷却倒计时。

### 2.3 枪械移动与命中

- 只有真正的连发激光枪使用 Hitscan：射线延伸到首个敌人或墙体；
- 左轮、自动手枪、冲锋枪、霰弹枪、狩猎步枪和突击步枪全部使用高速实体 Projectile；
- 实体子弹具有真实位置、弹速、寿命，并用连续扫掠检测敌人和墙体，避免高速穿透；
- 重力坍缩弹使用实体移动：朝鼠标目标点飞行；
- 重力弹途中碰撞敌人或墙体会提前坍缩；
- 未碰撞则在目标点坍缩；
- 所有攻击仍执行碰撞/命中判定。

## 3. 枪械系统范围

### 3.1 武器定义

当前枪池：

| 武器 | 射击性格 |
|---|---|
| 连发激光枪 | 高速、精准、初始武器 |
| 重力场枪 | 目标落点、牵引、范围坍缩 |
| 左轮手枪 | 均衡单发 |
| 自动手枪 | 高频倾泻 |
| 冲锋枪 | 极高射速、近距散布 |
| 霰弹枪 | 多弹丸扇形爆发 |
| 狩猎步枪 | 低射速、高伤、天然穿透 |
| 突击步枪 | 稳定中距自动火力 |

武器使用统一 `WeaponDefinition`、`WeaponInstance` 和 `WeaponRuntime`，不再在玩家代码中逐把硬编码。

### 3.2 品质与武器词条

- 白色：无词条；
- 蓝色：1 条词条；
- 橙色：2 条词条；
- 首批词条：校准、急速、双发、穿透；
- 重力场枪仍由宝箱固定提供橙色版本。

### 3.3 怪物枪械掉落

```text
怪物死亡
→ 按怪物类型判定掉枪
→ 按权重选择武器
→ 抽取品质和词条
→ 在世界中生成枪械掉落物
→ 玩家靠近后按 E 装备
```

- 远程怪掉枪率高于近战怪；
- 玩家拥有两个武器槽；
- 有空槽时，靠近掉落物并按 `E` 才会放入空槽；
- 两槽已满时不能拾取，枪械继续留在地面；
- `G` 丢弃当前武器并在玩家附近生成对应掉落物；
- 至少保留一把武器，不能丢弃最后一把；
- `1` / `2` 切换武器；
- 掉落物显示枪名、品质色和词条；
- 离开关卡时当前房间的所有掉落物清零。

## 4. 当前技术结构

```text
src/
├── config/
│   ├── demoConfig.ts
│   └── weaponDefinitions.ts
├── combat/
│   ├── Player.ts
│   ├── Enemy.ts
│   ├── IncomingAttack.ts
│   ├── Sfx.ts
│   └── Textures.ts
├── run/
│   ├── RunState.ts
│   └── RunRng.ts
├── scenes/
│   ├── MenuScene.ts
│   ├── RoomScene.ts
│   └── ResultScene.ts
└── ui/
    └── Overlay.ts
```

状态边界：

- `WeaponDefinition`：静态枪械数据；
- `WeaponInstance`：本局具体枪械、品质和词条；
- `RunState`：装备槽、Build、生命、印记和流程真源；
- `RoomScene`：敌人、枪械掉落物、碎片、弹丸和房间生命周期；
- UI：只展示状态和提交选择。

## 5. 迭代顺序

### Iteration 4：枪械基础与掉落

- 数据化八把枪；
- 两个武器槽和统一射击入口；
- 六种常规枪的差异化弹道；
- 怪物掉枪、品质、首批词条和拾取；
- HUD 展示两个槽位和当前武器。

**验收：** 一局中能拾取并切换多种枪；霰弹、冲锋枪、狩猎步枪和重力枪的观感明显不同。

### Iteration 5：规则说明与表现

- 印记说明常驻 HUD；
- 去除碎片吸附；
- 蓝色格挡展开、成功冲击环和蓝白反击光；
- 枪械拾取提示、品质色和词条标签。

**验收：** 新玩家不看策划案也能理解印记；成功格挡一眼可见；枪械掉落不会被误认为普通特效。

### Iteration 6：枪械深化

- 评估加入燃烧、电击、弹跳、爆炸等视觉型词条；
- 增加枪械替换选择，避免两槽满时自动覆盖；
- 调整枪械掉率、品质分布和各枪 DPS；
- 依据试玩反馈决定是否恢复弹夹与换弹。

## 6. 明确暂不进入

- 完整 15 词条和 4 个橙色特效；
- 备用弹药、弹夹与换弹；
- 武器商店价格和经济平衡；
- 局外武器解锁；
- 武器存档；
- 复杂背包页面；
- UE 正式开发框架。

本文档只保留当前有效决策；旧的三日赶工计划和“两把固定武器、无枪械掉落”等规则已经废止，不再作为开发上下文。
